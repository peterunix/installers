#!/bin/sh

dir=$(mktemp -d /tmp/$(basename $0).XXXXXX)
cd $dir
sudo apt install -y libxcb-icccm4-dev libxcb-ewmh-dev libxcb-keysyms1-dev
git clone https://github.com/baskerville/bspwm
sudo make -j $(nproc) install
